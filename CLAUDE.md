# Agent Instructions — fabric_flutter (Claude)

This file mirrors `AGENTS.md`. See that file for the full thin-pointer content — the guardrail on
not editing `.github/**` without explicit need, the required-reading order
(`.github/copilot-instructions.md` first, then each `.github/instructions/*.md` file's own
`applyTo` frontmatter to determine scope), the one-paragraph summary of the identity gate, change
scope guardrails, and deployment guardrail (cited to `.github/copilot-instructions.md` §0, §0.1,
§0.2 respectively), and the precedence rule.

Nothing in this file adds to or overrides `AGENTS.md` or `.github/copilot-instructions.md`. If the
two ever appear to diverge, `.github/copilot-instructions.md` wins.
